export interface AppSystemDesign {
  colors: {
    primary: string;
    secondary: string;
    tercary: string;
  };
}

export const appSystemDesign: AppSystemDesign = {
  colors: {
    primary: "#FFFFFF",
    secondary: "#FFC121",
    tercary: "#000000",
  },
};
